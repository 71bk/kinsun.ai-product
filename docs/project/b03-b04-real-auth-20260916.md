# B03／B04 真實登入驗收 — 2026-09-16

## 結果

本機 production frontend → BFF → Core → Supabase 真實登入驗收通過，沒有 Browser API
mock、身分注入或 session cookie 注入。使用新的隔離合成 campaign；所有測試帳號、憑證、
session 與短效授權已退場，合成事件及稽核保留。這完成 B03 類型／時間修正與 B04 來源
篩選的本機真實登入切片，並非完整 B03／B04、Wave 2 或 production deployment 結案。

- 程式基準：[PR #51](https://github.com/71bk/kinsun.ai-product/pull/51)，main `6bf89a8`。
- 合併後 [CI 35051354455](https://github.com/71bk/kinsun.ai-product/actions/runs/35051354455)
  已成功；不是本次新增 QA 工具的 CI 結果。
- Frontend 使用前一輪已驗證的 `6b5448e` production build，對應已合併的相同前端內容；
  Core 為既有 8000 服務，health／ready 200，schema `a5c7e9f1b324`。
- 新工作分支：`qa/b03-b04-real-auth-20260916`，由最新 main 建立並保留前次 migration 紀錄。
- 機器可讀證據：[JSON](b03-b04-real-auth-20260916.json)。

## Campaign 邊界

`b03-b04-real-auth-20260916` 建立一個隔離 DEMO tenant／日照單位、一位新日照照護員、
兩位合成長者（其中一位完全未授權）、兩筆最長兩小時 membership 與一筆短效照護關係。
登入使用新隨機密碼及 Kinsun 原生驗證，沒有改動或重啟先前 demo／QA 身分。

隔離的 policy／CARE_EVENT_EXTRACTION consent、已完成的合成 conversation，以及兩筆
舊來源語意的 VERIFIED 事件是 fixture 直接建立。這些不代表真實長者同意或 live Agent
提案／語音擷取。UNKNOWN 事件的 source_type／source_session_id 皆 NULL；另一筆
source_type=NULL、有 session 的 legacy 事件應歸屬 CONVERSATION_SESSION。

其餘四筆 MANUAL 候選是登入後由 authenticated BFF 呼叫 create command 真正建立。
三筆經 UI CORRECT；第四筆只用於撤權後提交，應保持 NEEDS_REVIEW v1。

## 實際驗證

| 項目 | 結果 |
| --- | --- |
| 真實帳密表單登入 | 成功，`/me` 200 且 actor ID 符合隔離帳號 |
| 建立人工候選 | 四次 create 201，DB source_type=MANUAL |
| 未授權／不存在長者 | 均回覆 404 |
| 類型＋時間修正 | MEAL → SLEEP、台北 21:30 → UTC 13:30，CORRECT 200，v1 → v2 |
| 相同 key 重送 | 200、response snapshot 相同；沒有多出 review／version／outbox |
| 真實舊分頁衝突 | 第二個分頁持有 v1，提交回 409；顯示重新載入提示且保留草稿 |
| 清除時間 | UI 送 explicit null，DB 與 audit after_time 均 NULL |
| 不修改時間 | Request 省略類型／時間欄位，保留原值及秒數 `01:10:45Z` |
| 三種來源 | UI MANUAL 3 筆、CONVERSATION_SESSION 1 筆、UNKNOWN 1 筆；ID 集合符合預期 |
| 清除篩選 | UI request 不帶 source_type，回到五筆正式事件 |
| 分頁與交集 | 真實 API limit=2 的前兩頁共四個不重複 ID；MANUAL＋SLEEP＋CORRECTED＋日期只得到修正事件 |
| 撤權後 UI | 將唯一照護關係提前到期，已開啟表單提交 404，姓名／事件／草稿／確認框卸載 |
| 撤權後 replay／GET | 已成功修正的 same-key replay 與清單 GET 均 404 |

瀏覽器流程共 13 組檢查通過。真實資料量只有六筆，未觸發 UI limit=100 的下一頁按鈕；
UI 載入下一頁後切換條件清除 cursor 的證據仍屬前一輪 synthetic Browser QA，不能與
本次 limit=2 的真實 API 分頁驗證混稱。

## DB／稽核核對與退場

- 最終六筆事件：兩筆 fixture VERIFIED v1、三筆 UI CORRECTED v2、一筆待覆核 v1。
- 共九個內容版本、三筆 review；前後類型／時間／版本值吻合三次 UI CORRECT。
- 七筆 outbox：四筆 candidate-created、三筆 corrected，各 aggregate／version 正確。
- 撤權與拒絕提交／重送前後，六筆事件及其所有版本、review、campaign outbox 的指紋相同：
  `6fdde68cde212546ecd29ee4466db55577f437ae9560a2f5fa30880ed67f2f63`。
- v1 指紋亦一致；此摘要不涵蓋其他 tenant 或全部 DB，不宣稱全資料庫零寫入。
- Retirement 已關閉 actor、撤銷 identity／password credential／App Session，提前結束
  membership、relationship、consent 與 policy 有效期。最終 live memberships=0、
  active sessions=0、active credential=false、live relationship=false。
- 私有 `.qa/.env.b03-b04-real-auth` 已移除帳密，只留 retired 標記及合成 IDs，不進 Git。
- 未執行 migration、reset、delete、truncate 或整合測試 fixture rebuild。

## 畫面與本機工具

英文介面在 375×812、390×844、430×932、1440×900 的真實修正表單均截圖並逐張檢查，
DOM 無水平溢出，類型／時間輸入、清除核取方塊及按鈕可用。另檢查 390px 撤權前後畫面，
拒絕後僅保留無權限提示。六張本機圖片位於 `.qa/local/b03-real-*.png`，不進 Git。
未重驗真機、reduced motion 或中文真實登入畫面；前次中文 synthetic QA 為獨立證據。

本次無產品 bug／runtime code 修正。新增 guarded fixture、真實 Browser 操作工具與
13 個離線安全測試；測試、Ruff lint／format、Node 語法檢查通過。CI rules 26 tests／
65 subtests、文件相對連結與 diff whitespace 通過；證據 JSON 未含憑證欄位。
完整前後端 suite 未重跑。
QA Frontend 3000 已停止，既有 Core 8000 保持原狀。

下一步為 B02 摘要來源回查、日界／超限資料與修正後重建驗收；修正歷史瀏覽、來源片段
導覽及匯出等其餘產品缺口仍依原 gap audit 追蹤。
